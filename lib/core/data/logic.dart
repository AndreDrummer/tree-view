

// Location
// if(location.hasParentId){
//   locationParent.add(location);
// }

// assets
// if(asset.sensorType != null) {
  // it is a component
  // if(!asset.hasParentId && !asset.hasLocationId) {
  //   nodeStart.add(asset);
  // }

    // if(asset.hasLocationId && !asset.hasParentId) {
      //   locationParent.add(asset);
    // }

// }


// if(asset.hasLocationId && asset.sensorType == null) {
//   locationParent.add(asset);
// }

// if(asset.hasParentId && asset.sensorType == null) {
//   assetParent.add(asset);
// }